{
  "name": "flow2api-pc-agent",
  "private": true,
  "type": "module",
  "description": "PC agent: WebSocket to agent-gateway + Playwright reCAPTCHA Enterprise (mirrors remote_browser / headed flow)",
  "scripts": {
    "start": "node agent.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "playwright": "^1.50.0",
    "ws": "^8.18.0"
  }
}
